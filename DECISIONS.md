# COUNTER — 설계 결정 기록 (Decision Log)

> **이 문서의 목적**: 설계를 바꾸고 싶어질 때 "왜 이렇게 되어 있는지"를 먼저 알기 위해서입니다.
> 아래 결정들은 대부분 **한 번 시도했다가 폐기한 경로**이거나, **겉보기에 개선처럼 보이지만 되돌리면 무너지는 것들**입니다.
> 좋은 의도로 되돌리기 쉬운 항목들이라 특히 조심하세요.

---

## D-01. 사람이 반례를 승인하는 단계 → **폐기**

**한때 이렇게 설계했었음**: 에이전트가 반례 후보를 찾으면 사람이 "이게 진짜 반례가 맞다"고 승인하고, 그 승인 라벨을 축적해 데이터 자산으로 삼는 구조.

**폐기 이유**: 대회 규칙이 "입력 1회 → 툴 호출 → 판단 → 출력까지 독립 수행, 단계마다 사람이 버튼을 눌러야 하는 구조는 에이전트로 인정하지 않음"이라고 명시합니다. 심사위원이 즉석 태스크로 새 광고를 던지는 순간(=캐시 미스)이 정확히 사람 승인이 필요한 경로가 되어 **자격 자체가 흔들립니다.**

**대신 채택**: 완전 자율 판정 + 판정이 나간 **이후**의 비동기 피드백(👍/👎). 피드백은 응답을 블로킹하지 않고, 이의가 임계치 이상 쌓이면 다음 조회 때 자동 재검증합니다.

> ⚠️ 되돌리지 마세요. "정확도를 위해 사람이 한 번 확인하면 좋지 않을까"는 자연스러운 생각이지만 실격 리스크입니다.

---

## D-02. 신뢰도 점수(%) → **의도적으로 만들지 않음**

경쟁 제품들은 "87% 신뢰도" 같은 숫자를 냅니다. 그들 설명에 따르면 이 숫자는 "몇 개의 독립 출처가 확인/부정하는가 + 출처의 신뢰도 + 증거가 직접적인가 정황적인가"를 합친 값입니다.

**문제**: 이 세 항목을 하나의 백분율로 합치는 정당한 함수가 존재하지 않습니다. 그럴듯해 보이지만 아무것도 측정하지 않는 숫자입니다. 심사위원(OpenAI Solutions Architect)이 "그 87%는 어떻게 계산됩니까"라고 물으면 답이 없습니다.

**대신**: 4값 이산 판정 + 근거 URL + 실행 쿼리 전문. 측정할 수 없는 것을 측정한 척하지 않는 것이 이 제품의 입장입니다.

> ⚠️ "사용자 경험을 위해 점수를 보여주면 좋겠다"는 제안이 나오면 이 항목을 인용해 거절하세요.

---

## D-03. "참/거짓" 판정 → **하지 않음. 반례 존재 여부만 보고**

우리는 "이 제품이 정말 국내 최초인지"를 인증할 수 없습니다. 원리적으로 부재 증명은 불가능합니다. 그래서 `NOT_REFUTED`는 "사실이다"가 아니라 **"우리가 실행한 이 N개 쿼리에서 반례를 찾지 못했다"**입니다.

이 비대칭성을 숨기지 않고 전면에 내세우는 것이 제품 논리의 핵심입니다.

> ⚠️ `NOT_REFUTED`의 사용자 문구를 "확인되었습니다", "문제없습니다"로 바꾸지 마세요. 제품이 무너집니다.

---

## D-04. OpenAI 내장 web_search 툴 → **쓰지 않음. 검색은 전부 LINER**

한때 "LINER=Scholar 전용, OpenAI web_search=일반 웹"으로 나누는 안을 검토했습니다.

**최종 결정**: 검색은 **전부 LINER**를 통합니다 (Web / Scholar 2개 모델).

**이유**:
1. LINER API Agent 트랙입니다. 검색의 절반을 OpenAI로 넘기면 "LINER가 이 제품의 핵심인가"라는 질문에 방어가 약해집니다.
2. 역할 경계가 훨씬 깔끔해집니다 — **LINER는 검색을 실행하고, OpenAI는 그 검색을 계획·평가·판정합니다.** 이 한 문장으로 Q&A가 끝납니다.
3. 검색 로그가 한 프로바이더로 통일되어 감사 로그와 델타 서치 구현이 단순해집니다.

---

## D-05. LINER Deep Research / Search Agent → **이번 스코프 제외**

**이유**:
- Deep Research는 자체적으로 하위 과제를 만들고 반복 검색·추론·인용 보고서를 생성합니다. 그 위에 우리 멀티에이전트(triage→hypothesis→search→verdict)를 올리면 심사위원이 "그래서 OpenAI 에이전트들은 실제로 무엇을 결정합니까?"라고 물었을 때 답이 궁색해집니다.
- 결제 직전이라는 실시간 경로에 다단계 리서치를 상시 투입하면 지연이 커져 Real-time Adaptability(25%)에 오히려 불리합니다.
- 우리에게 필요한 건 합성된 답변이 아니라 **URL과 발행일이 붙은 '문서의 존재'**입니다. Agent API는 그걸 요약해버립니다.

---

## D-06. FAISS → **pgvector로 통일**

벡터 저장소로 FAISS를 쓸지 pgvector를 쓸지 검토했습니다.

**결정: pgvector.**

**이유**:
- 카테고리 개수는 많아야 수십 개, claim canonical도 해커톤 규모에서는 수천 건 수준입니다. 별도 벡터 인덱스 프로세스를 세우는 비용 대비 이득이 거의 없습니다.
- 이미 claim / canonical / evidence / verdict가 전부 관계형 DB에 있습니다. **캐시 라우팅은 "같은 업종 파티션 안에서 벡터 검색"이라는 조인 성격의 쿼리**라, 벡터와 관계형이 분리되면 애플리케이션에서 수동 조인해야 합니다.
- 인덱스 파일 동기화·재빌드라는 실패 지점이 하나 줄어듭니다. 18시간 안에 이런 실패 지점을 늘릴 이유가 없습니다.

> 참고: 초기 기획 문서 중 일부에 FAISS와 SQLite가 언급된 버전이 있습니다. **그 문서는 구버전입니다.** 이 결정이 최신입니다.

---

## D-07. 업종 카테고리는 동적, claim_type은 고정

**동적 (에이전트가 즉석 생성 가능)**: `industry_category`
- 심사위원이 사전 정의에 없는 업종(장례서비스, 드론 방제 등)을 던져도 그 자리에서 새 카테고리를 만들어 정상 처리합니다. Real-time Adaptability 시연의 핵심 장면입니다.

**고정 (절대 동적 생성 금지)**: `claim_type`
- `claim_type`은 `falsifier_spec`과 연결되어 **REFUTED 판정 기준 자체를 결정**합니다. 이걸 LLM이 즉흥 생성하면 "무엇이 반례인가"의 기준이 매번 달라져서, 사람 검수도 없는 구조에서 유일한 정확성 방어선이 무너집니다.

> ⚠️ "카테고리를 동적으로 만들 수 있으면 클레임 타입도 그렇게 하면 되지 않나?"는 질문이 나오면 이 항목으로 답하세요. 둘은 성격이 다릅니다.

---

## D-08. 업종 파티셔닝의 목적은 스케일이 아니라 정확도

`claim_canonical` 매칭이 같은 `industry_category` 안에서만 일어나도록 강제한 이유는 "대규모 DB를 만들기 위해서"가 아닙니다.

**서로 다른 업종의 "국내 최초" 주장이 임베딩상 유사하다는 이유로 같은 canonical로 묶이면**, 화장품 클레임에 대한 판정이 가전 클레임에 재사용되는 오매칭이 발생합니다. falsifier_spec의 scope 요구사항이 사실상 무의미해집니다.

**부수효과**로 업종별 반례 원장(향후 B2B 확장 서사)과 확장성 서사를 얻습니다. 하지만 그건 부수효과이지 목적이 아닙니다.

**실무 주의**: 파티셔닝하면 카테고리 간 캐시 히트가 아예 안 생깁니다. 데모 데이터를 여러 업종에 얇게 뿌리면 `reuse_count`가 왜소해 보이므로 2~3개 업종에 몰아서 시드하세요.

---

## D-09. 델타 서치 (시간 간극만큼만 좁힌 재검색)

캐시가 오래되면 처음부터 다시 검색하는 대신, 이미 확인된 증거 위에 **간극 기간만** 좁혀서 재검색합니다.

**왜 중요한가**: 순수 캐싱은 경쟁사도 하루면 복제 가능한 로직입니다. 하지만 델타 서치는 **재현하려면 애초에 축적된 과거 증거가 있어야 성립**합니다. 오늘 새로 진입하는 경쟁사는 얹을 기반이 없어서 매번 풀 서치를 해야 합니다. 이게 순수 캐싱보다 한 단계 더 방어 가능한 논리입니다.

**전제 조건**: LINER가 날짜범위 필터를 지원해야 합니다. **미확인 상태입니다.** 지원하지 않으면 이 기능은 스코프 아웃하고 캐시를 fresh/full 2-state로 축소합니다 (`PRD.md` §10-4).

**트리거 구분**: 시간 간극 → 델타 서치 / 이의제기 누적 → 풀 재검색. 섞지 마세요 (`DB_SCHEMA.md` §2).

---

## D-10. 규제 근거 인용 시 주의 (발표·문구 작성 시)

두 개의 서로 다른 공정위 규정을 **절대 섞어서 인용하지 마세요.**

| 규정 | 상태 | COUNTER와의 관계 |
|---|---|---|
| 「추천·보증 등에 관한 표시·광고 심사지침」 개정 | **2026.6.1 시행됨** | AI 가상인물의 추천·보증광고 표시의무. **우리 유스케이스와 직접 관련 없음.** 배경 설명용으로만 |
| 「표시·광고 실증에 관한 운영」 개정안 | **2026.6.23~7.13 행정예고. 최종 고시 여부 8/1 기준 미확인** | 순위 표현·AI 성능 광고 사전실증. **이게 우리와 관련된 것** |

**"2026년 6월 1일부터 순위 광고와 AI 성능 광고 실증 의무가 시행됐다"는 문장은 사실관계 오류입니다.** UI 문구, README, 발표 자료 어디에도 쓰지 마세요.

안전한 표현: "행정예고됐다 / 방향이 공식화됐다". 최종 시행 여부는 확인되지 않았다고 정직하게 밝히는 편이, 나중에 심사위원이 직접 찾아봤을 때 걸리는 것보다 낫습니다.

---

## D-11. 임계값들은 전부 미검증 추정치

| 상수 | 초기값 | 상태 |
|---|---|---|
| `CATEGORY_REUSE_THRESHOLD` | 0.75 | 미검증. 시드 문구 10~20개로 스팟체크 필요 |
| `CANONICAL_THRESHOLD` | 0.85 | 미검증. 카테고리보다 엄격하게 잡은 것은 의도 |
| `default_ttl_days` | 유형별 14~180 | 미검증 운영 기본값 |
| dispute 임계 | 3건 & 30% | 미검증 |

**전부 설정으로 빼고 하드코딩하지 마세요.** 그리고 발표에서 이 숫자들을 "검증된 값"처럼 말하지 마세요. "초기값이고 튜닝 대상"이라고 정직하게 말하는 게 안전합니다.

---

## D-12. 이 프로젝트가 방어하지 않는 것 (정직한 한계)

Q&A에서 과장하면 무너집니다. 아래는 **인정하고 들어가는** 항목입니다.

- **강한 데이터 해자가 지금 있다고 주장하지 않습니다.** 있는 것은 오판정을 막는 결정론적 구조와 축적 기반 델타 서치입니다.
- **경쟁사 대비 정량 벤치마크(50~100건)를 수행하지 못했습니다.** 적대적 예시 3~5건의 정성적 결과만 있습니다. 물어보면 그대로 말하세요. 없는 데이터를 지어내지 마세요.
- **반증 탐색 자체는 저희가 처음이 아닙니다.** 팩트체킹 문헌에 멀티에이전트 증거 검색 선행 연구가 있습니다. 우리가 한 건 그걸 광고 최상급 주장 도메인에 좁히고, 4값 출력 계약과 결정론적 반례 기준으로 고정한 것입니다.
- 초기에는 API 조합이라 복제 가능성이 있습니다. 이걸 숨기지 않는 것이 오히려 신뢰를 만듭니다.

---

## D-13. 응답시간 목표: 1~2분 → 3~5분 소프트 실링으로 완화

**배경**: 초기에는 B2C 체감 속도를 이유로 1~2분을 목표로 잡았습니다. 이후 "실제로 작동하는 에이전트를 만드는 것"이 우선순위로 재확인되면서, LINER 리서치 속도가 기대만큼 안 나와도 큰 문제가 아니라는 방향으로 조정했습니다.

**최종 결정**: 클레임 1건당 목표 응답시간은 **3~5분 소프트 실링**입니다. "소프트"인 이유는 하드 데드라인으로 응답을 끊는 게 아니라, 아래 두 안전장치로 최악의 경우에도 무한정 대기하지 않게만 막는다는 뜻입니다.

1. **검색 예산 상한** (기존, `ARCHITECTURE.md` §3) — 쿼리 개수를 claim_type별로 제한. 시간이 아니라 "탐색 범위"를 통제하는 1차 안전장치.
2. **JOB_TIMEOUT_SECONDS** (신규, `ARCHITECTURE.md` §3.1) — RESEARCHING/EVALUATING 단계 누적 경과시간이 이 값을 넘으면 그 시점까지 확보된 candidate만으로 강제 종료. 기존 `DEGRADED` 패턴을 그대로 재사용합니다(새 상태 안 만듦). 기본값은 3~5분 범위 안에서 설정값으로 관리하고, 다른 임계값들과 마찬가지로 **미검증 추정치**입니다.

**S5 평가 폭도 같이 제한**: 응답시간과 별개로, S5(증거 적용가능성 평가, `gpt-5.6-sol` high effort)가 조기 종료 없는 `NOT_REFUTED` 경로에서 검색 결과 전량을 평가하도록 방치하면 시간과도 무관하게 비용이 무제한으로 커집니다. `claim_type`에 `max_evidence_per_query` 컬럼을 추가해 쿼리당 평가 대상 문서 수를 제한합니다 (`DB_SCHEMA.md` 참조). 이건 시간 여유와 무관하게 항상 유효한 안전장치입니다.

> ⚠️ **미해결 긴장 관계**: `BUILD_PLAN.md` §4의 데모 시나리오는 5개 시나리오를 "리허설용 3분"으로 스크립트하고 있습니다. 시나리오 1(캐시 미스 풀파이프라인)이 3~5분까지 걸릴 수 있다면, 라이브 데모에서 이 스크립트 타이밍이 그대로 안 맞습니다. 이 문서는 백엔드 응답시간 목표만 조정한 것이고, **데모 스크립트/리허설 타이밍을 어떻게 맞출지는 별도로 팀이 결정해야 합니다** (`BUILD_PLAN.md` §0 M6 참조).

---

## D-14. 팀 구조 변경: 별도 프론트엔드 팀원 → Streamlit 단일 배포로 흡수

**배경**: 원래 계획(`PRD.md` 구버전 §9)은 "우리=백엔드 에이전트+DB+이벤트 스트림 API, 다른 팀원=프론트엔드+DB 시각화"로 역할을 분리하고, 그 경계를 HTTP API + SSE 계약으로 고정하는 것이었습니다.

**최종 결정**: **Streamlit이 이 프로젝트의 최종 산출물**이고, 프론트엔드·DB 시각화 역할까지 이 저장소가 전부 흡수합니다. 배포 경로는 로컬 개발 → GitHub → Streamlit Community Cloud, DB는 Neon(PostgreSQL + pgvector)입니다.

**바뀐 것**:
1. `BUILD_PLAN.md` §1의 HTTP 엔드포인트 + SSE 계약 → **파이프라인 내부 함수 계약 + `trace_event` 테이블 폴링**으로 대체. SSE가 필요했던 이유(별도 프로세스로 돌아가는 팀원 프론트엔드에 실시간 push)가 사라졌고, 이미 설계돼 있던 `trace_event` 테이블이 폴링 기반으로도 동일한 요건(대회 규칙 3 — raw tool_call/tool_result를 가공 없이 세컨드 화면에 노출)을 만족시킵니다. 스키마 변경은 폴링 성능을 위한 인덱스 하나(`idx_trace_event_job_seq`)뿐입니다.
2. `ARCHITECTURE.md` §6 이벤트 스트림 설명 → "발행/구독"에서 "INSERT/폴링"으로 표현 변경. 이벤트 타입·순서 보장·`provider` 구분 규칙 등 **내용 자체는 그대로**입니다. 대회 요건은 전송 방식(SSE vs 폴링)이 아니라 "가공 없는 raw 이벤트가 실시간에 가깝게 보이는가"이므로 요건 충족 여부는 바뀌지 않습니다.
3. 세컨드 화면(대회 규칙 3)은 Streamlit 멀티페이지의 별도 페이지로 구현. 같은 Neon DB를 보는 두 번째 화면이라는 점에서 원래 의도("독립된 화면에서 심사위원이 직접 확인")를 그대로 만족합니다.

**바뀌지 않은 것**: N1~N6 불가침 규칙, 4값 판정 계약, REFUTED 게이트, 캐시/델타 서치 로직, 검색 예산, S0~S6 파이프라인 순서와 그 이유 — **전부 그대로**입니다. 이번 변경은 순수하게 "결과물을 어떻게 전달하는가"의 문제이고, 판정 로직이나 심사 배점 25%(Pipeline Architecture)에 해당하는 부분은 건드리지 않았습니다.

**왜 위험하지 않은가**: 원래 API 계약을 먼저 고정한 이유는 "다른 팀이 늦게 알면 통째로 막힌다"였는데, 이제 API를 소비하는 쪽(UI)과 만드는 쪽(파이프라인)이 같은 저장소·같은 사람이라 이 리스크 자체가 없어졌습니다. 다만 계약을 아예 없애지 않고 §1.1의 내부 함수 시그니처로 남겨둔 이유는, 계약이 없으면 파이프라인 코드와 UI 코드가 뒤섞여서 B01~B11의 단계별 검증 게이트를 독립적으로 테스트하기 어려워지기 때문입니다.

**미해결 항목 (`BUILD_PLAN.md` M7)**: Neon 무료 티어의 scale-to-zero 콜드스타트가 실측되지 않았습니다. Streamlit도 유휴 시 잠드는 특성이 있어, 데모 직전 두 서비스 모두 "깨우기" 지연이 겹칠 수 있습니다. 리허설 체크리스트에 데모 시작 전 warm-up 단계를 넣으세요.
